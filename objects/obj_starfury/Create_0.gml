SINGLETON

G.player = id;

spd = 0.7; // Pixels per frame
zone = noone;

primary_cooldown = 15; // How long before you can fire another shot
primary_cooldown_current = 0;
primary_max = 4; // How many shots on screen prevent more shots from being fired

bomb_dist = 30;

explosion = ps_explosion_player;
